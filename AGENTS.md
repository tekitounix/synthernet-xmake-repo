# AGENTS.md — synthernet-xmake-repo

ARM 組込み + UMI 消費用の自作 xmake パッケージリポジトリ。

## 規則

- パッケージ定義の変更は消費側 (umi 系) のビルドで検証。
- 検証済みの知見 (判断・手法・教訓) は本リポジトリに溜めず shiron へ ingest する (`kb ingest`)。
- 一次資料・データシートは knowledge (artifact:*) へ。本リポジトリには引用 ID で参照を置く。
